<%= simple_form_for(@column) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :pos %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
