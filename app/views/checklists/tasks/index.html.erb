<h1>Listing checklists_tasks</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @checklists_tasks.each do |checklists_task| %>
  <tr>
    <td><%= checklists_task.name %></td>
    <td><%= checklists_task.description %></td>
    <td><%= link_to 'Show', checklists_task %></td>
    <td><%= link_to 'Edit', edit_checklists_task_path(checklists_task) %></td>
    <td><%= link_to 'Destroy', checklists_task, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Task', new_checklists_task_path %>
